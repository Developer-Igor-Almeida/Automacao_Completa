version: "3.9"

services:
  app:
    build: .
    container_name: automacao_completa
    ports:
      - "8501:8501"
    volumes:
      - .:/app          # monta o projeto inteiro — devs editam e já reflete
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped